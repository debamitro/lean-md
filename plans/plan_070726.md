# Features to be added

1. Add a save button or menu item for writing out the file to disk
2. Add a button or menu item for hiding the editor, so that this becomes a viewer only
3. Add a command-line switch called --readonly where the editor is always hidden
